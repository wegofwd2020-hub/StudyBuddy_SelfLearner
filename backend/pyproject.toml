# StudyBuddy Q backend — tool configuration

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "."]

[tool.ruff.lint]
select = [
    "E", "W", "F", "I", "B", "C4", "UP",
    "S",   # security
    "T20", # no print()
    "RUF",
]
ignore = [
    "E501",   # handled by formatter
    "B008",   # FastAPI Depends pattern
    "S101",   # assert in tests
    "RUF001", # ambiguous unicode in strings — EN DASH in prompts (matches OnDemand)
    "RUF002", # ambiguous unicode in docstrings — editorial, not functional
    "RUF003", # ambiguous unicode in comments — editorial, not functional
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S", "T20"]
"src/core/owner_cli.py" = ["T20"]  # CLI — stdout is its interface (ADR-018 owner CLI)

[tool.ruff.lint.isort]
known-first-party = ["backend"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["src"]
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
fail_under = 70

# scripts/

Operational helpers for **StudyBuddy Q**.

## Planned scripts (empty until needed)

| Script | Purpose |
|---|---|
| `sync-from-ondemand.sh` | Vendor sync from StudyBuddy_OnDemand. See ADR-002 |
| `check-no-key-leaks.sh` | Grep CI helper — fail if any committed file contains a literal `sk-ant-` substring |
| `dev_start.sh` | Local Docker Compose orchestration (mirrors OnDemand pattern). Backend + Redis + Postgres (v1.1+) |

## Status

⏳ Empty. First scripts land with the MVP PR.
